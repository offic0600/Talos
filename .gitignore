__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.env
*.db

# Acceptance tests are now parameterized — they require environment variables
# (TALOS_GITLAB_URL, TALOS_PILOT_REPO, etc.) to run, with no hardcoded values.
# They are part of the platform, not environment-specific.

# docs/ contains implementation records (acceptance reports, incident RCAs,
# design documents with real environment info). It is "具体实施" not "通用平台".
docs/
